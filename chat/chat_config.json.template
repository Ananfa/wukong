{
    "ip": "0.0.0.0",         // 内网提供rpc服务的监听ip
    "port": 13000,           // rpc服务端口
    "ioRecvThreadNum": 2,    // IO接收线程数（为0表示在主线程中进行IO接收，注意：接收和发送不能都在主线程中）
    "ioSendThreadNum": 2,    // IO发送线程数（为0表示在主线程中进行IO发送，注意：接收和发送不能都在主线程中）
    "nexus": {                                  // 服务注册发现服地址
        "host": "127.0.0.1",
        "port": 17000
    }
}